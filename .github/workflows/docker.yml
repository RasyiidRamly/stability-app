name: Build & Push Docker Image

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to DockerHub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build image
        run: docker build -t zurgigas/stability-app:latest .

      - name: Push image
        run: docker push zurgigas/stability-app:latest

      #- name: Setup kubeconfig
        #run: |
          #mkdir -p $HOME/.kube
          #echo "${{ secrets.KUBECONFIG }}" > $HOME/.kube/config

      - name: Deploy to Kubernetes (disabled - local cluster not reachable )
        run: 
            # kubectl set image deployment/stability-app app=zurgigas/stability-app:latest
             # kubectl rollout status deployment/stability-app
         echo "Skipping deployment:"
         echo "Cluster is local (Docker Desktop) and not accessible from GitHub Actions"
         echo "Will enable after moving cluster to VPS/cloud

      #- name: Run container
        #run: |
          #docker run -d -p 3000:3000 --name test-app zurgigas/stability-app:latest
          #sleep 5

      #- name: Health check
       # run: |
         # curl -f http://localhost:3000/health

      #- name: Stop container
        #run: docker rm -f test-app